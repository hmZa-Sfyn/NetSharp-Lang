using netsharp "MathUtils.ns";
using System;
class Program {
  shared void Main() {
    var i = 0;
    var product = 1;
    do product = MathUtils.Multiply(product, ++i); while (i < 4);
    Console.WriteLine($"Product: {product}");
  }
}